<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd
    http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    ">

    <!-- 扫描component -->
    <context:component-scan base-package="info.puton.customize.hsb.scheduler" />
    <context:component-scan base-package="info.puton.customize.hsb.crawler"/>

    <!-- Quartz -->

    <bean name="crawlPortalJobDetail" class="org.springframework.scheduling.quartz.JobDetailFactoryBean">
        <property name="jobClass" value="info.puton.customize.hsb.scheduler.job.CrawlPortalJob" />
        <property name="durability" value="true" />
    </bean>

    <!-- Run the job every 5 seconds -->
    <bean id="cronTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="crawlPortalJobDetail" />
        <!--<property name="cronExpression" value="0/10 * * ? * *" />-->
        <property name="cronExpression" value="* * 10/10 ? * *" />
    </bean>

    <!-- Scheduler factory bean to glue together jobDetails and triggers to Configure Quartz Scheduler -->
    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="jobDetails">
            <list>
                <ref bean="crawlPortalJobDetail" />
            </list>
        </property>
        <property name="triggers">
            <list>
                <ref bean="cronTrigger" />
            </list>
        </property>
    </bean>

</beans>
